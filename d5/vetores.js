const numeros = [1,2,3,4,5];

for (let i = 0; i < numeros.lenght; i++) {
let resultado = numeros[i] * 2;
let resto = resultado % 3;

console.log ("O indice atual é: " + i);
if (resto == 0) {
console.log("O numero " + numero[1] + " é multiplo de 3"
}}